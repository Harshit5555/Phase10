'use client';
import {useEffect,useState} from 'react';
import {CircleHelp,Volume2,VolumeX} from 'lucide-react';
import {Dialog,DialogContent,DialogTitle,DialogDescription} from '@/components/ui/dialog';
import {PHASES} from '@/config/game';
export function playSound(kind='card'){if(typeof window==='undefined'||localStorage.getItem('tenfold-sound')!=='on')return;try{const ctx=new AudioContext();const oscillator=ctx.createOscillator(),gain=ctx.createGain();oscillator.connect(gain);gain.connect(ctx.destination);oscillator.type='sine';oscillator.frequency.setValueAtTime(kind==='phase'?660:440,ctx.currentTime);oscillator.frequency.exponentialRampToValueAtTime(kind==='phase'?880:220,ctx.currentTime+.12);gain.gain.setValueAtTime(.035,ctx.currentTime);gain.gain.exponentialRampToValueAtTime(.001,ctx.currentTime+.18);oscillator.start();oscillator.stop(ctx.currentTime+.2);setTimeout(()=>ctx.close(),300);}catch{}}
export default function Chrome({children}:{children?:React.ReactNode}){const [help,setHelp]=useState(false),[sound,setSound]=useState(false);useEffect(()=>setSound(localStorage.getItem('tenfold-sound')==='on'),[]);return <><header className="topbar"><a className="brand" href="/"><span className="brand-mark">t.</span>tenfold<span className="brand-dot">✳</span></a>{children??<span className="top-tag">GOOD CARDS. GREAT COMPANY.</span>}<div className="header-actions"><button className="icon-btn" aria-label={sound?'Mute sound':'Enable sound'} title={sound?'Mute sound':'Enable sound'} onClick={()=>{localStorage.setItem('tenfold-sound',sound?'off':'on');setSound(!sound);if(!sound)playSound();}}>{sound?<Volume2 size={19}/>:<VolumeX size={19}/>}</button><button className="subtle" onClick={()=>setHelp(true)}><CircleHelp size={18}/> How to play</button></div></header><Dialog open={help} onOpenChange={setHelp}><DialogContent className="help-dialog"><DialogTitle className="dialog-title">A good hand starts here.</DialogTitle><DialogDescription>Ten phases. One goal: finish them all.</DialogDescription><div className="rules-grid"><div><b>1. Draw one</b><p>Take the top card from the deck or discard pile.</p></div><div><b>2. Make your phase</b><p>Select cards and place them into the staging groups. Play the whole objective at once.</p></div><div><b>3. Discard one</b><p>Choose one card to end your turn. After completing your phase, you can add cards to any matching group on the table.</p></div><div><b>Go out. Move up.</b><p>Empty your hand to end the round. Everyone who played their phase advances. Finish phase ten to win; lowest score breaks ties.</p></div></div><div className="rule-example"><b>Sets</b> Same numbers, any colors: 3 · 3 · 3<br/><b>Runs</b> Consecutive numbers: 4 · 5 · 6 · 7<br/><b>Wilds</b> Fill a gap or stand in for any number/color. In a played run, their value stays fixed.<br/><b>Points</b> 1–9: 5 points · 10–12: 10 · Wild: 25. Fewer is better.</div><details><summary>See all ten phases</summary><ol>{PHASES.map((p,i)=><li key={i}>{p.name}</li>)}</ol></details></DialogContent></Dialog></>}
